# Watermark-GUI
This is a simple bash script using zenity for the GUI that helps you to watermark any number of files with text of your choice.

## Usage
To run the program:

```
wm.sh 
```

To get help:

```
wm.sh -h OR wm.sh --help
```
